
// Defining the possible actions allowed in the game
export type ActionsType = "rock" | "paper" | "scissors" | null;

export interface ActionSetType {
    rock: string;
    paper: string;
    scissors: string;
  }